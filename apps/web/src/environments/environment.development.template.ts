// Copy this file to environment.development.ts and fill in real values.
// environment.development.ts is gitignored — never commit secrets.
export const environment = {
  production: false,
  apiUrl: '/api',
  adminApiKey: 'REPLACE_WITH_ADMIN_API_KEY_FROM_ENV',
};
